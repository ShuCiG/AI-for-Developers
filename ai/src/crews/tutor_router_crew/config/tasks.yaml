router_task:
  description: >
    You are a language tutor router. Given the user's latest message and recent conversation history, classify the intent.

    Allowed intents (language learning only):
    - translation: user asks how to say a word/phrase in another language (e.g., "how do you say hello in Polish?")
    - new_word: user asks for a new word to learn, or vocabulary suggestion
    - grammar: user asks about grammar, verb conjugation, sentence structure
    - example_sentences: user wants example sentences or usage context
    - practice: user wants to practice writing and get corrections
    - save_word: user wants to add a word to their list / flashcard deck
    - cultural: user asks about formal vs informal, cultural context of language use
    - off_topic: request is NOT about language learning. Examples include:
      * Cooking instructions ("how to cook X", "how to make X", "recipe for X")
      * General how-to questions not about language ("how to do X", "how to use X")
      * Math problems, coding help, general knowledge questions
      * Opinions on non-language topics, casual chat unrelated to learning

    User message: {message}

    Recent conversation history (oldest first):
    {history}

    CRITICAL RULES:
    - "How to cook X" or "How to make X" questions are ALWAYS off_topic, NOT translation. These are cooking instructions, not language learning.
    - "How to say X in Y" is translation (language learning).
    - "How to cook X" is off_topic (cooking instruction, not language).
    - If the request asks for instructions on how to DO something (cook, make, use, etc.), it is off_topic unless it's clearly about language learning.
    - If the request is clearly about learning or using a language (translation, grammar, vocabulary, practice, examples, culture), choose the matching intent.
    - If the request is off-topic (recipes, cooking instructions, math, coding, general knowledge, non-language opinions), you MUST return intent "off_topic".
    - Optionally set source_language and target_language if the user has specified or implied them.

    Return a JSON object with: intent, reasoning, source_language (or null), target_language (or null).
  expected_output: >
    JSON with intent (one of the allowed values), reasoning (short), source_language (string or null), target_language (string or null).
  agent: tutor_router
